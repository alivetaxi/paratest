spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:paradb
    username: sa
    password: 
  h2:
    console:
      enabled: true
mybatis:
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    root: debug